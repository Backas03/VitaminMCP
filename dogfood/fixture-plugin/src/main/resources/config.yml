# Which fault this server is running with. One at a time — see dogfood/SCENARIOS.md.
#
# This is the shipped default, not a statement about any running server. The live value is
# whatever sits in that server's plugins/DogfoodFixture/config.yml, and the plugin says which
# one it enabled in the startup log. Trust the log; a round has already nearly been wasted on
# trusting this file.
#
#   none              everything works
#   async-reply       /top
#   silent-refusal    /shop
#   join-lockout      breaking a block just after joining
#   silent-listener   the join kit
#   disabled-feature  /kit
scenario: none

kit:
  enabled: true

# The roster. Every player is on a team.
teams:
  Tester1: red
